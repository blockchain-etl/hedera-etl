hedera:
  dedupe:
    projectId:
    datasetName:
    transactionsTableName:
    stateTableName:
    metricsEnabled: false
    scheduling:
      enabled: false # Dedupe runs are manually invoked in tests

spring:
  cloud:
    gcp:
      credentials:
        location:
